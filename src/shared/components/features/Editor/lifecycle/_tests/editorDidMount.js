import * as React from 'react'
import { shallow } from 'enzyme'

import { editorDidMount } from '../editorDidMount'

const { it, expect } = global

it('works and stuff', () => {
	expect(true).toBe(true)
})
