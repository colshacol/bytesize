import * as React from 'react'

const { Provider, Consumer } = React.createContext({
	error: 'state not provided in this context'
})



export const LessonProvider = inject(stateSelector)(props) => {
	return (
		<
	)
})