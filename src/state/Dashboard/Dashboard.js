import { types } from 'mobx-state-tree'
import queryString from 'query-string'

const model = {}

const actions = self => {
	return {}
}

const views = self => {
	return {}
}

export default types
	.model(model)
	.actions(actions)
	.views(views)
